<%= provide(:title,'Password dimenticata') %>

<br><h1>Password dimenticata?</h1><br>
<center><h5>Inserisci l'indirizzo email a cui inviare le istruzioni per il recupero della password</h5></center><br>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, class: "form-control" %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Invia mail", class: "btn btn-primary" %>
  </div>
<% end %>
</div>
</div>
<br><br><br>
